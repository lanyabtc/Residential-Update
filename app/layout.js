export const metadata = {
  title: "FW Portal — Elitez",
  description: "Quarterly Foreign Worker Residential Information Update",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
